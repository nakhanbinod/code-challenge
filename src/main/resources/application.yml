# Database Configuration
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: username
    url: jdbc:mysql://192.168.0.104:3306/code-challenge
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
  jackson:
    default-property-inclusion: non_empty
server:
  port: 8000
